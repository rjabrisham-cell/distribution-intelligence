# دستورالعمل همکاری در پروژه DIP

این قواعد در ریشه پروژه و تمام زیرپوشه‌های آن اعمال می‌شوند.

## ۱. برنامه و تأیید پیش از اجرا

- قبل از هر اقدام اجرایی، برنامه کار، فایل‌های هدف، دستورهای احتمالی و اثر مورد انتظار را اعلام کن.
- تا دریافت تأیید صریح کاربر، هیچ اقدام اجرایی یا دستور Terminal اجرا نکن؛ این محدودیت شامل بررسی وضعیت Git و اجرای تست‌ها نیز می‌شود.
- سکوت یا درخواست توضیح کاربر به معنای تأیید نیست.
- تأیید فقط برای محدوده اعلام‌شده معتبر است. برای تغییر برنامه، فایل‌های هدف یا دستورها، دوباره تأیید بگیر.

## ۲. محدودیت تغییرات

- بدون تأیید صریح کاربر، هیچ فایل یا پوشه‌ای ایجاد یا تغییر نده.
- بدون تأیید صریح، هیچ تغییری در Git، Docker، دیتابیس، Migration یا تنظیمات ایجاد نکن.
- نصب یا به‌روزرسانی وابستگی‌ها و تغییر فایل‌های قفل نیز نیازمند تأیید صریح است.

## ۳. ممنوعیت عملیات مخرب

- هرگز `git reset --hard` یا `git clean` اجرا نکن.
- هرگز فایل، جدول یا داده را حذف نکن.
- هیچ دستور مخرب، از جمله حذف volumeهای Docker یا بازنویسی اجباری تاریخچه Git، اجرا نکن.
- اگر انجام درخواست به عملیات مخرب نیاز دارد، توقف کن و راه جایگزین غیرمخرب پیشنهاد بده.

## ۴. عملیات Git

- Commit، Push، Merge، Rebase و ساخت یا تغییر شاخه فقط با تأیید صریح کاربر مجاز است.
- تأیید ویرایش فایل‌ها، مجوز انجام عملیات Git نیست.
- تغییرات موجود کاربر را حفظ کن؛ آن‌ها را بازنویسی، برنگردان یا بدون اجازه وارد Commit نکن.

## ۵. اندازه و سازگاری تغییرات

- تغییرات را کوچک، موضعی و محدود به درخواست تأییدشده نگه دار.
- از الگوها و معماری فعلی پروژه پیروی کن.
- از بازنویسی گسترده فایل‌های تست‌شده، بازآرایی غیرضروری و تغییرات نامرتبط خودداری کن.
- اگر تغییر گسترده ضروری شد، دلیل و محدوده آن را اعلام کن و پیش از اجرا تأیید بگیر.

## ۶. معماری پروژه

معماری زیر را حفظ کن:

`Jinja2 → FastAPI Router → Service → Repository → PostgreSQL/PostGIS`

- Jinja2 مسئول نمایش است.
- Router مسئول دریافت درخواست و هدایت آن به Service است.
- منطق کسب‌وکار در Service قرار می‌گیرد.
- دسترسی به دیتابیس در Repository انجام می‌شود.
- از دسترسی مستقیم قالب‌ها یا Routerها به دیتابیس و انتقال منطق کسب‌وکار به آن‌ها خودداری کن.

## ۷. بررسی و تست

- پیش از تغییر، برنامه بررسی وضعیت Git و تست‌های مرتبط را اعلام کن و برای اجرای آن تأیید بگیر.
- پس از تأیید، وضعیت Git و نتیجه تست‌های مرتبط را به‌عنوان وضعیت اولیه بررسی کن.
- بعد از تغییر، تست‌های مرتبط را در محدوده تأییدشده دوباره اجرا کن.
- تست‌هایی که داده یا تنظیمات را تغییر می‌دهند، باید با توضیح اثر و تأیید صریح اجرا شوند.
- اگر تست قابل اجرا نبود، دلیل را گزارش کن؛ بدون شواهد، موفقیت تست را اعلام نکن.

## ۸. شکست تست‌ها

- تست شکست‌خورده را پنهان، حذف یا برای عبور ظاهری غیرفعال نکن.
- شکست تست، علت مشخص یا احتمالی و ارتباط آن با تغییرات را گزارش کن.
- علت تأییدنشده را قطعی بیان نکن.
- پیش از هر اصلاح، برنامه اصلاح را اعلام کن و تأیید صریح بگیر.

## ۹. حفاظت از اطلاعات

- محتوای فایل `.env`، اطلاعات ورود، کلیدها، رمزها، داده مشتری، محتوای `uploads` و نسخه‌های پشتیبان را نمایش، Commit یا منتشر نکن.
- اطلاعات حساس را وارد پاسخ، گزارش، خروجی Terminal، لاگ یا مستندات نکن.
- برای توضیح تنظیمات فقط از نام متغیرها و مقادیر نمونه غیرواقعی استفاده کن.
- پیش از Commit مجاز، فایل‌های انتخاب‌شده را از نظر وجود اطلاعات حساس بررسی کن؛ این بررسی نباید محتوای حساس را نمایش دهد.

## ۱۰. Docker، Alembic و دیتابیس

- پیش از اجرای هر دستور Docker، Alembic یا هر دستور تغییر‌دهنده دیتابیس، دستور دقیق را اعلام کن.
- محیط هدف، اثر دستور بر سرویس‌ها، ساختار دیتابیس و داده‌ها، و احتمال قطع سرویس را به زبان ساده توضیح بده.
- تا دریافت تأیید صریح همان دستور، آن را اجرا نکن.
- تأیید ساخت یا ویرایش Migration، مجوز اجرای آن نیست.
- ممنوعیت عملیات مخرب در این بخش نیز برقرار است.

## ۱۱. شیوه پاسخ‌گویی

- پاسخ‌ها فارسی، کوتاه، روشن و قابل استفاده برای کاربر غیر‌برنامه‌نویس باشند.
- اصطلاحات فنی ضروری را به زبان ساده توضیح بده.
- پیش از اجرا، مشخص کن چه کاری انجام می‌شود، چرا لازم است و چه اثری دارد.
- میان پیشنهاد، اقدام انجام‌شده و نتیجه تأییدشده تفاوت روشن بگذار.

## ۱۲. گزارش پایان اقدام

در پایان هر اقدام اجرایی، این موارد را گزارش کن:

- فایل‌های ایجادشده یا تغییرکرده و دلیل تغییر.
- تست‌های اجراشده و نتیجه آن‌ها.
- نتیجه اقدام و مشکلات یا کارهای باقی‌مانده.
- وضعیت Git بر اساس آخرین بررسی مجاز، شامل شاخه و تغییرات موجود.

اگر بررسی یا تستی اجرا نشده است، صریحاً اعلام کن. برای تکمیل گزارش، دستور خارج از محدوده تأییدشده اجرا نکن.
