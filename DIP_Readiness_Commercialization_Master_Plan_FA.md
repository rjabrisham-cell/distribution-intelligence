# سند مرجع تجاری‌سازی Readiness در DIP

**نسخه:** ۱.۰  
**تاریخ:** ۱۵ شهریور ۱۴۰۵  
**وضعیت:** سند زنده؛ مبنای طراحی و توسعه مراحل بعدی

## ۱. هدف محصول

Readiness باید فایل خام فروشگاه‌های مشتری را به یک دارایی قابل استفاده در توزیع هوشمند تبدیل کند. خروجی این مرحله فقط امتیاز یا گزارش فنی نیست؛ بلکه مجموعه‌ای از فروشگاه‌های اعتبارسنجی‌شده، دارای موقعیت جغرافیایی، متصل یا افزوده‌شده به Master، دارای کد یکتای پایدار و آماده ورود به Decision Engine است.

مسیر اصلی محصول:

```text
ورود کاربر → ایجاد پروژه → Upload → Validation → Matching →
تکمیل موقعیت با FIMAP → Readiness → تخصیص کد یکتا →
نمایش نقشه → تحویل فایل نهایی → Decision Engine
```

## ۲. تعریف نهایی وضعیت فروشگاه

### ۲.۱. آماده و متصل به Master

- رکورد ورودی با یک Store موجود تطبیق قطعی دارد.
- کد یکتای همان Store به مشتری برگردانده می‌شود.
- مختصات معتبر از فایل مشتری یا Master تأمین شده است.

### ۲.۲. آماده و جدید

- رکورد در Master پیدا نشده است.
- اطلاعات لازم برای عملیات و مختصات معتبر دارد.
- Store جدید با کد یکتای پایدار ایجاد می‌شود.
- منبع، پروژه، Batch، زمان ایجاد و منشأ مختصات ثبت می‌شود.

### ۲.۳. نیازمند بررسی

- تطبیق چندمعنایی است؛ یا
- مختصات مشکوک/نامعتبر است؛ یا
- FIMAP چند موقعیت پیشنهادی نزدیک ارائه کرده است؛ یا
- اطلاعات برای تصمیم قطعی کافی نیست.

### ۲.۴. بدون مختصات

- رکورد پس از بررسی و Geocoding هنوز مختصات ندارد.
- این رکورد روی نقشه نمایش داده نمی‌شود.
- فقط در شمارنده و فهرست «موارد نیازمند اصلاح» نمایش داده می‌شود.

### ۲.۵. ردشده

- رکورد به دلیل خطای قطعی و غیرقابل استفاده وارد بسته Decision نمی‌شود.

## ۳. قاعده مهم Readiness

پیدا نشدن در Master به‌تنهایی مانع آمادگی نیست. فروشگاهی که مختصات معتبر و اطلاعات عملیاتی کافی دارد می‌تواند به‌عنوان Store جدید ثبت، کدگذاری و آماده شود.

مختصات صحیح الزام مهم آمادگی مکانی است، اما رکورد جدید باید با منشأ و سطح اطمینان ثبت شود تا تاریخچه Master قابل ردیابی بماند.

## ۴. مدل مالکیت داده

| موجودیت | مسئولیت |
|---|---|
| `Store` | هویت مرجع، مختصات معتبر و کد یکتای DIP |
| `CompanyStore` | نام، کد داخلی، تلفن و اطلاعات اختصاصی مشتری |
| `ProjectCompanyStore` | عضویت فروشگاه در پروژه و وضعیت Matching/Readiness |
| `ImportBatch` | منشأ فایل و اجرای پردازش |
| فایل ورودی | شواهد اصلی ارسال‌شده توسط مشتری |

اصول کد یکتا:

- پس از ایجاد تغییر نمی‌کند.
- اجرای مجدد Readiness کد جدید یا Store تکراری نمی‌سازد.
- اگر رکورد قبلاً کد DIP دارد، Matching مستقیم از همان کد آغاز می‌شود.
- تخصیص کد و ایجاد/اتصال Store در تراکنش کنترل‌شده انجام می‌شود.

## ۵. نقش FIMAP

FIMAP فقط Base Map نیست و در چهار حوزه استفاده می‌شود:

1. نمایش نقاط پروژه و لایه‌های مرجع؛
2. تبدیل آدرس فارسی به مختصات؛
3. Reverse Geocoding و کنترل موقعیت؛
4. فراهم‌کردن قابلیت‌های مکانی مشترک برای Readiness، Decision، Distribution و Results.

تمام تعامل‌ها باید از مسیر موجود زیر تکمیل شوند و Adapter موازی ساخته نشود:

```text
backend/app/services/geo/adapters/fimap_adapter.py
```

## ۶. پرسش‌ها و اقلام موردنیاز از برنامه‌نویس FIMAP

1. لایه فعلی سوپرمارکت‌های FIMAP از چه دیتابیس، فایل یا API خوانده می‌شود؟
2. نام Source و Layerهای این سوپرمارکت‌ها چیست؟
3. آیا لایه مرجع را می‌توان روشن/خاموش و محدوده زوم آن را تنظیم کرد؟
4. API رسمی Forward Geocoding آدرس فارسی چیست؟
5. آیا Batch Geocoding برای ۱۰۰۰ رکورد یا بیشتر وجود دارد؟
6. قرارداد پاسخ شامل مختصات، آدرس استاندارد، استان، شهر، منطقه، محله، امتیاز اطمینان و گزینه‌های پیشنهادی چیست؟
7. در پاسخ چندمعنایی، گزینه‌ها و دلایل امتیاز چگونه دریافت می‌شوند؟
8. API رسمی Reverse Geocoding چیست؟
9. روش رسمی `fitBounds` یا دسترسی کنترل‌شده به Map Object چیست؟
10. روش رسمی Cluster کردن نقاط DIP چیست؟
11. چگونه نقاط با دایره‌های کوچک و رنگ‌های مختلف، بدون Marker تصویری، نمایش داده شوند؟
12. قرارداد Click Event و Popup چیست؟
13. سقف درخواست، Timeout، Retry و محدودیت تعداد نقاط چیست؟
14. آیا Health Check و نسخه‌بندی API/SDK وجود دارد؟
15. مجوز استفاده تجاری از Base Map، Geocoding و لایه سوپرمارکت‌ها چیست؟

## ۷. طراحی صفحه Readiness

اجزای مشترک Journey باید بدون حذف حفظ شوند:

```jinja2
{% include "partials/shared/breadcrumb.html" %}
{% include "partials/shared/project_header.html" %}
{% include "partials/shared/journey_stepper.html" %}
{% include "partials/shared/current_step.html" %}
```

### KPIهای اصلی

- کل رکوردهای فایل
- آماده توزیع
- تطبیق‌یافته با Master
- فروشگاه جدید و کدگذاری‌شده
- نیازمند بررسی
- بدون مختصات

### قرارداد بصری نقشه

| رنگ | مفهوم | نمایش روی نقشه |
|---|---|---|
| سبز | متصل به Master و آماده | بله |
| آبی | Store جدید، کد جدید و آماده | بله |
| نارنجی | دارای مختصات و نیازمند بررسی | بله |
| خاکستری | دارای مختصات ولی وضعیت تعیین‌نشده | بله |
| قرمز | مختصات یا داده دارای خطای قطعی | فقط در صورت وجود مختصات |
| بدون مختصات | فاقد نقطه مکانی | خیر؛ فقط فهرست و شمارنده |

نقشه باید روی Bounds تمام نقاط قابل نمایش پروژه تنظیم شود، در زوم پایین Cluster داشته باشد و امکان فیلتر بر اساس وضعیت و نمایش/عدم نمایش لایه مرجع FIMAP را فراهم کند.

Popup هر فروشگاه:

- نام فروشگاه؛
- کد مشتری؛
- کد یکتای DIP؛
- منشأ مختصات: مشتری، Master یا FIMAP؛
- وضعیت Matching؛
- وضعیت Readiness؛
- دلیل بازبینی.

## ۸. خروجی قابل تحویل به مشتری

### فایل نهایی کدگذاری‌شده

ستون‌های اصلی مشتری حفظ و ستون‌های زیر اضافه می‌شوند:

```text
dip_store_code
longitude
latitude
coordinate_source
master_match_status
readiness_status
readiness_reason
```

### فایل موارد نیازمند اصلاح

- شماره ردیف اصلی؛
- اطلاعات ارسالی فروشگاه؛
- مشکل شناسایی‌شده؛
- مختصات موجود یا پیشنهادی؛
- اقدام موردنیاز مشتری.

## ۹. طرح حساب‌ها و دسترسی تجاری MVP

| قابلیت | Trial | Professional | Business |
|---|---:|---:|---:|
| سقف رکورد هر پروژه | ۱٬۰۰۰ | ۳٬۰۰۰ | ۵٬۰۰۰ |
| Upload/Validation/Matching/Readiness | دارد | دارد | دارد |
| مشاهده نقشه و نتیجه | کامل | کامل | کامل |
| دانلود فایل نهایی | قفل | دارد | دارد |
| ورود به Decision | قفل | دارد | دارد |
| اولویت صف | پایین | عادی | بالا |
| کاربران تیمی | ندارد | محدود | چندکاربره |

Trial فقط پس از رسیدن موفق پروژه به Readiness و تولید نتیجه قابل مشاهده مصرف می‌شود. Upload خراب، شکست پردازش یا اختلال سرویس خارجی Trial را مصرف نمی‌کند.

قفل دانلود و Decision باید در Backend اعمال شود، نه فقط با مخفی‌کردن دکمه در HTML.

## ۱۰. ده گام اجرایی تجاری‌سازی

1. بررسی مدل فعلی `Account` و `entitlement_service.py`؛
2. تثبیت Trial و دو Plan پولی؛
3. ورود و تأیید شماره موبایل؛
4. اعمال سقف ۱٬۰۰۰ رکورد برای Trial در Import؛
5. ثبت مصرف Trial فقط پس از Readiness موفق؛
6. نمایش نتیجه کامل Readiness و نقشه؛
7. قفل Backend برای دانلود Excel و ورود به Decision؛
8. طراحی صفحه ارتقای حساب؛
9. تکمیل فایل خروجی کدگذاری‌شده؛
10. اتصال پرداخت و فعال‌سازی Entitlement.

## ۱۱. استفاده از پروژه CodeIgniter موجود

پروژه PHP CodeIgniter دارای دو دارایی اثبات‌شده است:

- اتصال به سامانه پیامک و ارسال OTP؛
- اتصال به درگاه بانکی و دریافت نتیجه پرداخت.

### راهبرد پیشنهادی MVP

این پروژه فعلاً بازنویسی کامل نمی‌شود. قراردادهای سالم آن استخراج و از طریق Adapter/API در DIP استفاده می‌شوند.

مالکیت داده باید روشن باشد:

- DIP مالک `Account`، Plan، Entitlement، Project و وضعیت Trial است.
- CodeIgniter در نقش Bridge اتصال به SMS و بانک عمل می‌کند.
- دیتابیس دو سامانه مستقیماً به هم متصل نمی‌شود.
- اطلاعات محرمانه بانک و پیامک در سرویس دارای اتصال باقی می‌ماند.

### جریان OTP پیشنهادی

```text
DIP → درخواست OTP با شناسه پیگیری → CodeIgniter/SMS → موبایل
کاربر → کد OTP → DIP/Bridge → نتیجه تأیید امضاشده → ایجاد Session در DIP
```

### جریان پرداخت پیشنهادی

```text
DIP → ایجاد سفارش پرداخت با شناسه یکتا → CodeIgniter/Bank
Bank Callback → Verify در CodeIgniter → Webhook امضاشده به DIP
DIP → ثبت Payment → فعال‌سازی Entitlement
```

الزامات امنیتی و پایداری:

- امضای درخواست و Webhook؛
- شناسه یکتای تراکنش و Idempotency؛
- عدم اعتماد به Redirect مرورگر بدون Verify بانکی؛
- ثبت Log بدون ذخیره OTP و اطلاعات حساس؛
- Timeout و Retry کنترل‌شده؛
- جلوگیری از فعال‌شدن دوباره اشتراک در Callback تکراری.

پس از رسیدن MVP به درآمد، می‌توان Adapterهای SMS و Payment را مستقیماً در Python پیاده‌سازی کرد؛ اما این بازنویسی شرط عرضه نسخه اول نیست.

## ۱۲. استفاده از سرویس‌های موجود و منع کد موازی

| نیاز | مسیر موجود برای بررسی و تکمیل |
|---|---|
| مجوز و Plan | `services/entitlement_service.py` |
| Import و سقف رکورد | `services/import_service.py` |
| خروجی Excel | `services/excel_service.py` |
| Matching فروشگاه | `services/store_matching_service.py` و `services/matching/` |
| Validation | `services/validation_service.py` |
| Audit/Readiness | `services/audit/audit_runner.py` و `report_builder.py` |
| مختصات | `services/audit/coordinate_validator.py` |
| استانداردسازی آدرس | `services/audit/address_standardizer.py` |
| هماهنگی Geo | `services/geo/geo_service.py` |
| FIMAP | `services/geo/adapters/fimap_adapter.py` |
| HTTP/Retry/Cache | زیرپوشه‌های موجود `geo/clients` و `geo_cache.py` |
| وضعیت پروژه | `services/project_lifecycle_service.py` |

اصل توسعه: ابتدا کد موجود خوانده و جریان واقعی ترسیم می‌شود؛ سپس همان سرویس‌ها تکمیل می‌شوند. ایجاد سرویس جدید فقط زمانی مجاز است که مسئولیت واقعاً جدیدی وجود داشته باشد و هیچ مسیر موجودی مالک آن نباشد.

## ۱۳. ترتیب اجرای فنی Readiness

1. استخراج جریان فعلی Import تا Readiness از کد موجود؛
2. تثبیت Statusها و قرارداد کد یکتا؛
3. تکمیل Matching موجود؛
4. تکمیل `fimap_adapter.py` براساس پاسخ تیم FIMAP؛
5. اتصال Geo Result به `matching_preparation_service.py`؛
6. تکمیل نتیجه در `audit_runner.py` و `report_builder.py`؛
7. Upsert تراکنشی Store و تخصیص کد؛
8. تکمیل نقشه و فهرست موارد بدون مختصات؛
9. تولید فایل خروجی در `excel_service.py`؛
10. اعمال Entitlement و Gateهای تجاری.

## ۱۴. سناریوهای پذیرش MVP

1. فایل تهران با مختصات کامل؛
2. فایل سراسری با بخشی از مختصات؛
3. فایل فقط دارای آدرس و نیازمند Geocoding؛
4. بارگذاری مجدد فایل دارای کد DIP؛
5. اجرای Trial با ۱٬۰۰۰ رکورد؛
6. تلاش Trial برای عبور از سقف؛
7. ارتقا، پرداخت موفق و بازشدن دانلود/Decision؛
8. Callback تکراری بانک بدون فعال‌سازی دوباره؛
9. شکست FIMAP بدون مصرف Trial؛
10. تطبیق کامل شمارنده‌های UI، دیتابیس و Excel.

## ۱۵. تصمیم‌های باز

- قالب دقیق و الگوریتم تولید `dip_store_code`؛
- حداقل فیلدهای عملیاتی Store جدید؛
- سیاست اعتماد به مختصات ارسالی مشتری؛
- مدت و تعداد پروژه‌های هر Plan پولی؛
- تعریف Queue و محاسبه زمان انتظار؛
- قرارداد API پروژه CodeIgniter؛
- پاسخ فنی FIMAP به پرسش‌های بخش ۶؛
- سیاست تأیید و ارتقای Storeهای جدید در Master.

## ۱۶. پاسخ تیم FIMAP و تصمیم‌های حاصل

### ۱۶.۱. امکانات تأییدشده

- Base Map از دیتابیس محلی `iran.mbtiles` و تایل‌های برداری PBF تأمین می‌شود.
- مسیر مصرف تایل‌ها `/data/iran/{z}/{x}/{y}.pbf` است.
- Source اصلی Style با نام `openmaptiles` یا در برخی نسخه‌ها `fimap` معرفی شده است؛ نام دقیق باید از Style فعال محیط اجرا خوانده شود.
- Source-Layer فروشگاه‌های مرجع `poi` است و سوپرمارکت‌ها با کلاس‌های `shop`، `grocery` و `supermarket` فیلتر می‌شوند.
- لایه مرجع قابلیت مخفی/نمایان‌شدن و تغییر Zoom Range دارد.
- نقاط DIP می‌توانند به‌صورت GeoJSON، Circle Layer و Cluster نمایش داده شوند.
- Click Event و Popup روی ویژگی‌های GeoJSON قابل پیاده‌سازی است.
- Attribution نقشه باید در UI حفظ شود.

### ۱۶.۲. امکاناتی که در FIMAP فعلی وجود ندارند

- Forward Geocoding آدرس فروشگاه؛
- Batch Geocoding؛
- Confidence Score و چند پاسخ پیشنهادی برای آدرس مبهم؛
- Reverse Geocoding پستی؛
- Health/Version Endpoint رسمی.

نتیجه: نمایش و تعامل نقشه از FIMAP دریافت می‌شود، اما تکمیل مختصات آدرس‌ها باید از مسیر Providerهای موجود DIP در `services/geo` انجام شود. پیش از انتخاب یا افزودن Provider جدید، `nominatim_adapter.py`، `photon_adapter.py`، `geoserver_adapter.py`، `fimap_adapter.py` و `geo_service.py` باید بررسی شوند.

### ۱۶.۳. موارد نیازمند اثبات پیش از کدنویسی

1. باید مشخص شود خروجی `new fimap(...)` مستقیماً تمام متدهای Mapbox مانند `addSource`، `addLayer`، `fitBounds` و `on` را ارائه می‌کند یا Wrapper جداگانه است.
2. اگر Wrapper است، روش رسمی دسترسی به Map Object باید از کد واقعی `map_value.js` استخراج شود.
3. شناسه دقیق لایه سوپرمارکت مرجع، مانند `poi-level-supermarket`، باید در Style فعال محیط تأیید شود و از حدس یا نام نمونه استفاده نشود.
4. ادعای پردازش روان ۵۰٬۰۰۰ نقطه باید با تست Performance روی مرورگر و سخت‌افزار هدف اثبات شود؛ این عدد فعلاً معیار تضمین‌شده محصول نیست.
5. نبود Rate Limit به معنی نبود محدودیت ظرفیت نیست. برای Geocoding و پردازش‌های سنگین باید Queue، Timeout و Concurrency تعریف شود.
6. دریافت `200 OK` از ریشه TileServer فقط Liveness را نشان می‌دهد. Health Check آینده باید دسترسی Style، یک Tile نمونه و وابستگی‌های ضروری را نیز بررسی کند.

### ۱۶.۴. تصمیم اجرایی نقشه Readiness

- Base Map و لایه مرجع از FIMAP؛
- نقاط پروژه از GeoJSON تولیدشده در DIP؛
- رنگ و وضعیت نقاط تحت مالکیت DIP؛
- Popup و انتخاب نقطه در Client Map Layer؛
- کلاستر در Readiness به‌صورت پیش‌فرض استفاده نمی‌شود، زیرا ترکیب وضعیت‌های سبز، آبی، نارنجی و قرمز پیام تشخیصی صفحه را پنهان می‌کند؛
- `fitBounds` روی تمام نقاط دارای مختصات؛
- رکوردهای بدون مختصات فقط در فهرست کنار نقشه؛
- نمایش لایه مرجع FIMAP به‌صورت Toggle و نه مخلوط غیرقابل تشخیص با نقاط پروژه؛
- Geocoding از Service Layer موجود DIP و نه از Template یا JavaScript صفحه.

### ۱۶.۵. تصمیم اجرایی Geocoding

ترتیب بررسی و استفاده از داشته‌های موجود:

```text
geo_service.py
→ geo_provider_factory.py
→ Provider/Adapter موجود
→ geo_scoring_engine.py
→ geo_cache.py
→ نتیجه استانداردشده
→ matching_preparation_service.py
→ Readiness
```

تا قبل از بررسی این مسیر، هیچ میکروسرویس یا Adapter جدید Geocoding ساخته نمی‌شود.

## ۱۷. نتیجه ممیزی پوشه موجود `services/geo`

### ۱۷.۱. بخش‌های قابل حفظ و توسعه

- مدل‌های دامنه، Rule Registry، Scoring Engine، Builder و Cache پایه قابل استفاده‌اند.
- تفکیک Provider، Adapter، Client، Mapper و Config جهت معماری مناسبی دارد.
- `GeoValidationService` مسیر هماهنگ‌کننده فعلی است و باید تکمیل شود؛ سرویس موازی ساخته نمی‌شود.
- `GeoProviderFactory` قابل استفاده است، اما در کد تحویلی هیچ ثبت عملیاتی Provider برای آن مشاهده نشد.

### ۱۷.۲. شکاف‌های قطعی

1. `FimapAdapter` به endpointهای `/api/validate`، `/api/validate/coordinate`، `/api/geocode`، `/api/reverse`، `/api/search` و `/api/health` متکی است؛ طبق پاسخ رسمی تیم FIMAP این endpointها در نسخه فعلی وجود ندارند. بنابراین این Adapter در وضعیت فعلی عملیاتی نیست.
2. قابلیت‌های اعلام‌شده FIMAP در کد، از جمله Forward/Reverse Geocoding، با امکانات واقعی FIMAP همخوان نیست و باید اصلاح شود.
3. `NominatimAdapter`، `PhotonAdapter` و `GeoServerAdapter` اسکلت‌اند و متدهای اصلی آن‌ها `NotImplementedError` می‌دهند.
4. `GeoValidationService` فقط آدرس یا مختصات موجود را اعتبارسنجی می‌کند؛ آدرس فاقد مختصات را Geocode نمی‌کند و مختصات جدیدی به جریان Matching برنمی‌گرداند.
5. Contract فعلی مدل مستقلی برای «کاندیدای ژئوکدینگ» شامل مختصات، Confidence، آدرس نرمال‌شده، Provider و Provenance ندارد.
6. Factory خالی است و مسیر روشن و اثبات‌شده‌ای برای انتخاب Provider از Config تا اجرای واقعی دیده نشد.
7. دو مسیر هم‌پوشان HTTP/Retry وجود دارد: منطق داخلی `BaseGeoAdapter` و همچنین `clients/http_client.py`/`retry_policy.py` به‌همراه `HttpGeoProvider`. پیش از توسعه باید یک مسیر canonical انتخاب و دیگری حذف یا ادغام شود.
8. Cache فعلی فقط in-memory و process-local است؛ برای Batch، چند Worker و ازسرگیری پردازش کافی نیست.
9. تست‌های FIMAP همگی Skip هستند و Integration Testها فقط Mock Provider را می‌آزمایند؛ در نتیجه اتصال تولیدی آزموده نشده است.
10. فایل `tests/test_retry_policy.py` دارای خطای نحوی/تورفتگی است و کل مجموعه تست تحویلی در وضعیت فعلی سالم نیست.

### ۱۷.۳. تصمیم معماری

- FIMAP در حال حاضر Provider نمایش نقشه، Tile و لایه مرجع است؛ نه Provider ژئوکدینگ.
- اعتبارسنجی عددی و محدوده‌ای مختصات باید در DIP و بدون تماس شبکه انجام شود.
- ژئوکدینگ باید با تکمیل یکی از Adapterهای موجود انجام شود؛ اولویت بررسی با Provider قابل استقرار و مجاز در محیط تجاری است.
- خروجی ژئوکدینگ مستقیماً به مدل Store نوشته نمی‌شود؛ ابتدا کاندیدا، Confidence و Provenance ثبت و سپس براساس آستانه پذیرش تأیید می‌شود.
- رکورد فاقد مختصات روی نقشه نمایش داده نمی‌شود و فقط در فهرست اقدام/خطا قرار می‌گیرد.
- پس از تأیید مختصات و Matching، تخصیص کد یکتا و Upsert در Master باید تراکنشی و idempotent باشد.

### ۱۷.۴. ترتیب اصلاح بدون ایجاد کد موازی

1. اصلاح تست شکسته و فعال‌کردن Contract Testهای واقعی؛
2. یکپارچه‌کردن مسیر HTTP/Retry/Exception؛
3. تصحیح Capabilityهای FIMAP و محدودکردن Adapter آن به قابلیت‌های واقعی؛
4. افزودن مدل استاندارد `GeocodeCandidate` و نتیجه Batch به Contract موجود؛
5. تکمیل یک Adapter موجود برای Forward Geocoding؛
6. تکمیل Factory و Config برای انتخاب Provider و Fallback؛
7. افزودن Batch، Queue، Timeout، Retry و Cache پایدار؛
8. اتصال نتیجه به `matching_preparation_service.py` و جریان فعلی Readiness؛
9. ثبت Provenance و تصمیم پذیرش مختصات؛
10. تست End-to-End از Import تا نقشه و Excel خروجی.

## ۱۸. تصمیم نهایی تجربه بصری Readiness

- هدف نقشه Readiness تشخیص وضعیت هر رکورد است، نه صرفاً نمایش حجم یا تراکم فروشگاه‌ها.
- تمام نقاط قابل‌نمایش در یک GeoJSON Source و بدون Cluster رندر می‌شوند تا در تمام زوم‌ها وضعیت خود را حفظ کنند.
- رنگ سبز برای رکورد آماده و متصل به Master، آبی برای رکورد آماده جدید، نارنجی برای نیازمند بازبینی و قرمز برای نامعتبر است.
- رکورد فاقد مختصات روی نقشه قرار نمی‌گیرد و در فهرست اقدام جداگانه نمایش داده می‌شود.
- کاربر می‌تواند وضعیت‌ها را مستقل فیلتر کند و شمارنده هر وضعیت از همان GeoJSON پروژه محاسبه می‌شود.
- کلیک روی نقطه، آن را برجسته و علت وضعیت، شناسه شرکتی، کد Master و درصدهای کیفیت را نمایش می‌دهد.
- `fitBounds` فقط روی نقاط قابل‌نمایش و معتبر برای محدوده ایران اجرا می‌شود و رکوردهای پرت نباید محدوده اولیه نقشه را خراب کنند.
- کلاسترینگ برای صفحات Decision، Distribution و Results یا مجموعه‌داده‌های حجیم حفظ می‌شود؛ در Readiness فقط پس از طراحی کلاستر چندوضعیتی و اثبات ارزش بصری قابل بازگشت است.
- هیچ DOM Marker و هیچ تغییر مصنوعی مختصات برای جداسازی نقاط هم‌مکان ایجاد نمی‌شود.

### ۱۷.۵. فایل‌های لازم برای تعیین نقطه اتصال نهایی

برای طراحی تغییرات دقیق بعدی، بدون حدس و بدون بازنویسی موازی، باید این بخش‌های موجود بررسی شوند:

- کل پوشه `services/matching/`؛
- `services/store_matching_service.py`؛
- `services/audit/audit_runner.py`؛
- `services/audit/coordinate_validator.py`؛
- `services/audit/address_standardizer.py`؛
- مدل‌های Store، Master Store، Import Row/Batch و Match؛
- تنظیمات Environment و Dependency Injection مرتبط با Geo؛
- در صورت وجود، Worker/Queue و Migrationهای مرتبط.
